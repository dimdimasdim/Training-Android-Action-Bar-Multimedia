<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ActionBarMultimedia" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/color_sage</item>
        <item name="android:statusBarColor">@color/color_sage_light</item>
    </style>

    <style name="Theme.ActionBarMultimedia" parent="Base.Theme.ActionBarMultimedia" />
</resources>